{
  "name": "@cloakwp/backend-starter",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "dev": "docker-compose up -d",
    "stop": "docker-compose down",
    "composer": "docker-compose run composer",
    "postinstall": "docker-compose run composer create-project"
  }
}