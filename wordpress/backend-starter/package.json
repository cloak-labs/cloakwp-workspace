{
  "name": "@cloakwp/backend-starter",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "docker-compose up -d",
    "stop": "docker-compose down",
    "composer": "docker-compose run composer",
    "init": "docker-compose run composer create-project"
  }
}
